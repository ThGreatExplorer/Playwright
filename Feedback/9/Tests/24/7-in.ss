(
  (tmodule testone
    (class run (a)
      (method spin() 90.0)
      (method spin() 80.0))
    ( ((a Number)) ((spin () Number) (spin () Number)) )
  )
  (tmodule testone
    (class run (a)
      (method spin() 90.0)
      (method spin() 80.0))
    ( ((a Number)) ((spin () Number) (spin () Number)) )
  )
  (import testone)
  (def x 5.0)
  x)
